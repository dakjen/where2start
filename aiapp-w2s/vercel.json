{
  "buildCommand": "echo 'Starting Vercel build...' && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/(.*)"
    }
  ]
}